"""class Solution:
    def coinChange(self, coins: List[int], amount: int) -> int:
        coins.sort(reverse=True) 
        cnt = 0  # Count of coins used
        
        for coin in coins:
            if amount == 0:
                break  # No more amount left to form
            if coin <= amount:
                # Use as many of this coin as possible 
                #coins=[5,2,1] , amount=11
                count = amount // coin # 11/5=2
                cnt += count #cnt=2 (5 coins=2)
                amount -= count * coin # 11-{2(5 coins=2)*5}
        
        # If amount is not 0, return -1 (it's not possible to form the amount)
        return cnt if amount == 0 else -1 """

class Solution:
    def coinChange(self, coins: List[int], amount: int) -> int:
        dp = [amount + 1] * (amount + 1)
        dp[0] = 0

        for a in range(1, amount + 1):
            for c in coins:
                if a - c >= 0:
                    dp[a] = min(dp[a], 1 + dp[a - c])
        return dp[amount] if dp[amount] != amount + 1 else -1
